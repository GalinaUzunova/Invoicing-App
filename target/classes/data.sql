-- Optional local seed data template.
-- Current application.properties uses spring.sql.init.mode=never, so this file is not executed by default.
-- Enable only for local development after replacing values and password hashes with safe development data.

-- insert into users (first_name, last_name, email, password_hash, role, enabled, created_at, updated_at)
-- values ('Demo', 'Owner', 'demo@example.com', '<bcrypt-hash>', 'ROLE_USER', true, current_timestamp, current_timestamp);
